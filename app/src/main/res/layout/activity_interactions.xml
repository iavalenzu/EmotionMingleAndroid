<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                tools:context="com.gadgeteer.efelunte.emotionmingle.InteractionsActivity">


    <Spinner
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/spinner_contacts"
        android:layout_gravity="center_vertical"
        android:layout_margin="10dp"
        android:spinnerMode="dialog"
        android:theme="@style/Base.Widget.AppCompat.DropDownItem.Spinner"/>

    <Spinner
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/spinner_contacts"
        android:id="@+id/spinner_leafs"
        android:layout_gravity="center_vertical"
        android:layout_margin="10dp"
        android:spinnerMode="dialog"
        android:theme="@style/Widget.AppCompat.DropDownItem.Spinner"/>

    <Button
        android:id="@+id/button_save_relation"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:text="Guardar"
        android:layout_below="@+id/spinner_leafs"
        android:layout_margin="10dp"
        />



</RelativeLayout>
